const db = require('../lib/config/db')
const queryStr = require('../lib/query')
const codeMsg = require('../lib/res')

exports.createMemo = async (uid, pid, memo) => {
}

exports.deleteMemo = async (uid, pid) => {
}

exports.listMemo = async (uid, pid) => {
}