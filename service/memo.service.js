const db = require('../lib/config/db')
const queryStr = require('../lib/query')
const codeMsg = require('../lib/res')

exports.createMemo = async (userid, pid, memo) => {
}

exports.deleteMemo = async (userid, pid) => {
}

exports.listMemo = async (userid, pid) => {
}